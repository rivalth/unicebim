## UniCebim - Environment Variables
#
# Copy this file to `.env.local` and fill in values from your Supabase project:
# Supabase Dashboard → Project Settings → API

# Public (safe to expose to the browser; used by Supabase client)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: Used to build absolute email redirect links (fallback if request origin is not available)
# Example: http://localhost:3000
NEXT_PUBLIC_SITE_URL=

# Optional: App version shown in /api/health (non-secret)
NEXT_PUBLIC_APP_VERSION=

# Server-only (optional; only needed for admin/server maintenance tasks)
# SUPABASE_SERVICE_ROLE_KEY=


